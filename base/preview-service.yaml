apiVersion: v1
kind: Service
metadata:
  name: preview-svc
  labels:
    app: harshapp
spec:
  selector:
    app: harshapp
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      nodePort: 30100
  type: NodePort
